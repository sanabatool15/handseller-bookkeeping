# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-or-anon-key

# JWT verification (tokens issued by your auth provider / Supabase Auth)
JWT_SECRET=change-me-to-a-real-secret
JWT_ALGORITHM=HS256

# App
APP_ENV=local
CORS_ORIGINS=http://localhost:3000
